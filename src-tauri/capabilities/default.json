{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default permissions for AI Codec",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
